<UserControl x:Class="Pindelisten.PindelisteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pindelisten"
             local:ViewModelLocator.AutoHookedUpViewModel = "True" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <HierarchicalDataTemplate x:Key = "familieTemplate" DataType="{x:Type local:PindelisteViewModel}" ItemsSource="{Binding Path=Familier}">
            <StackPanel Orientation="Horizontal">
                <TextBlock  Text="{Binding Path=Navn}" FontWeight="Bold" FontSize="18"/>
            </StackPanel>
        </HierarchicalDataTemplate>
    </UserControl.Resources>

    <TreeView ItemsSource = "{Binding Familier}" ItemTemplate = "{StaticResource familieTemplate}"/>
</UserControl>
